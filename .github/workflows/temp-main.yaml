# TEMPORARY DEBUGGING VERSION - main.yml / checkup-final.yaml
name: Checkup Final DEBUG
on:
  workflow_dispatch:
jobs:
  appsec-android-native-job: # Or whichever job fails first
    name: DEBUG - appsec-android-native-job
    runs-on: ubuntu-latest
    # outputs: # Comment out outputs temporarily
    #   status: ${{ steps.call_reusable.outputs.status }}
    #   repo_name: 'docs-appsec-android-native'
    #   error_details: ${{ steps.call_reusable.outputs.error_log }}
    steps:
      - name: Checkout Repository Code
        uses: actions/checkout@v4

      # - name: Verify Workflow File Exists # You can comment out the debug ls now
      #   run: |
      #     ... ls commands ...
      #   shell: bash

      - name: Call MINIMAL Reusable Workflow # Call the simplified version
        id: call_reusable
        uses: ./.github/workflows/temp-reuse.yaml # Path remains the same
        with:
          ibe: false # Provide valid inputs
          iae: false
          targetRepo: 'digital-ai/docs-appsec-android-native' # Provide valid inputs

      - name: Confirm Call Step Finished
        run: echo "Minimal reusable workflow call step completed."

  # Comment out ALL other jobs temporarily to isolate the issue
  # deploy-job:
  #   ...
  # release-job:
  #   ...
  # platform-job:
  #   ...
  # appsec-final-job:
  #   ...
  # check_failures:
  #   ...